<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog Application" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest %>
    <%= tag.link rel: "manifest", href: "/manifest.json" %>

    <!-- Favicon -->
    <link rel="icon" type="image/png" sizes="16x16" href="<%= asset_path('blog-icon.png') %>">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= asset_path('blog-icon.png') %>">
    <link rel="icon" type="image/png" sizes="48x48" href="<%= asset_path('blog-icon.png') %>">
    <link rel="icon" type="image/png" sizes="500x500" href="<%= asset_path('blog-icon.png') %>">

    <!-- Apple Touch Icon -->
    <link rel="apple-touch-icon" sizes="500x500" href="<%= asset_path('blog-icon.png') %>">

    <%# Styles and JS %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <% if logged_in? %>
  <p>Welcome, <%= current_user.email %> | <%= link_to 'Logout', logout_path, method: :delete %></p>
<% else %>
  <p><%= link_to 'Login', login_path %> | <%= link_to 'Sign Up', signup_path %></p>
<% end %>
    <%= yield %>
  </body>
</html>
