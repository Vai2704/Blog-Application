<H1> Blog Application </H1>

<a href = "posts/new"> Create new post</a>
<%  @post.each do |post|%>
<div>
    <a href = "posts/<%= post.id%>"> 
    <h2><%= post.title%></h2>
    </a>
    <p><%= post.content%> </p>
    <hr>
    </div>
<% end%>