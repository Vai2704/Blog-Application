<!DOCTYPE html>
<html>
<head>
    <title>View Post</title>
    <link rel="stylesheet" th:href="@{/css/blog-styles.css}">
    <style>
        
        body {
            font-family: sans-serif;
            margin: 20px;
            padding: 0;
            background-color: #f9f9f9;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            border-bottom: 1px solid black;
            padding-bottom: 10px;
        }

        .post-container {
            width: 60%;
            margin: 20px auto;
            padding: 20px;
            border: 1px solid black;
            border-radius: 5px;
            background-color: white;
        }

        .comments-container {
            width: 60%;
            margin: 20px auto;
            padding: 15px;
            border: 1px solid black;
            border-radius: 5px;
            background-color: white;
        }

        .comment {
            border-bottom: 1px solid #ddd;
            padding: 10px 0;
        }

        .comment:last-child {
            border-bottom: none;
        }

        .comment strong {
            color: #333;
        }

        .comment p {
            font-size: 16px;
            margin: 5px 0;
        }

        .comment-form {
            width: 60%;
            margin: 20px auto;
            padding: 15px;
            border: 1px solid black;
            border-radius: 5px;
            background-color: white;
        }

        .comment-form input[type="text"] {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            border: 1px solid #ddd;
            border-radius: 3px;
        }

        .comment-form input[type="submit"] {
            margin-top: 10px;
            padding: 10px;
            background-color: white;
            color: black;
            border: 1px solid black;
            cursor: pointer;
            border-radius: 5px;
        }

        .post-title {
            font-size: 24px;
            font-weight: bold;
        }

        .post-content {
            margin-top: 15px;
            font-size: 18px;
        }

        .buttons {
            text-align: center;
            margin-top: 20px;
        }

        .buttons a, .buttons form {
            display: inline-block;
            margin: 5px;
        }

        .edit-button, .delete-button {
            text-decoration: none;
            color: black;
            border: 1px solid black;
            padding: 15px 20px;
            border-radius: 5px;
            cursor: pointer;
        }

        .edit-button,.delete-button {
            background-color: white;
        }

        a.back-button {
            display: block;
            text-align: center;
            margin-top: 20px;
            text-decoration: none;
            color: black;
            border: 1px solid black;
            padding: 10px;
            width: 150px;
            margin: 20px auto;
        }

        a.back-button:hover {
            background-color: #f0f0f0;
        }
    </style>
</head>
<body>

    <h1>View Post</h1>

    <div class="post-container">
        <div class="post-title"><%= @post.title %></div>
        <div class="post-content"><%= @post.content %></div>
    </div>

    <div class="buttons">
        
        <a href="<%= edit_post_path(@post) %>" class="edit-button">Edit</a>
        
        <form action="<%= post_path(@post) %>" method="delete" style="display:inline;">
            <input type="hidden" name="_method" >
            <button type="submit" class="delete-button" onclick="return confirm('Are you sure you want to delete this post?');">
                Delete
            </button>
        </form>
    </div>

    <a href="/" class="back-button">← Back to Posts</a>
    <hr>
    <%= render 'comments/comments'%>
    <%= render 'comments/form'%>

</body>
</html>